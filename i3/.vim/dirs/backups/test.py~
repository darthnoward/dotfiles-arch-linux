import cv2
import numpy as np

cap = cv2.VideoCapture(1)

while cap.isOpened():
     ret, frame = cap.read()
     cv2.imshow('window',frame)
     if cv2.waitKey(1) & 0xFF == ord('q'):
         break
     if not ret:
         break
cap.release()
cv2.destroyAllWindows()
